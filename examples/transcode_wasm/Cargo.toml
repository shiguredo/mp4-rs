[package]
name = "transcode_wasm"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
# 非同期処理用のコンビネータ (join_all など) に使用する
futures = "0.3"
# JSON の入出力に使用する
nojson = "0.3"
shiguredo_mp4 = { "path" = "../../" }
